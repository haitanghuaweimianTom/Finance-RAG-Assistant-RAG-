# 📊 金融研报RAG问答助手 (Finance RAG Assistant)

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)
[![API](https://img.shields.io/badge/LLM-Qwen2.5--7B--Instruct-orange.svg)](https://siliconflow.cn/)
[![Framework](https://img.shields.io/badge/Framework-Streamlit-red.svg)](https://streamlit.io/)

** 结合金融业务洞察（Domain Knowledge）与大模型 RAG 技术 **，专为券商研报深度解读设计的智能工具。

---

## 🌟 项目亮点

- **金融深度定制**：针对研报长难句、专业术语（PE/PB、毛利率、DCF）进行针对性 Prompt 工程优化。
- **高性能 RAG 架构**：集成 **BGE-M3** 多语言向量模型与 **Rerank** 二次打分机制，显著提升检索精度。
- **开箱即用**：为非技术背景用户封装了全自动部署脚本 (`.bat`)，实现零基础本地运行。
- **增量更新机制**：采用基于 **MD5 哈希内容校验** 的入库算法，支持海量研报的去重增量入库。

---

## 🛠️ 技术架构

本项目基于 **RAG (Retrieval-Augmented Generation)** 思想构建：

1.  **数据层**：利用 `pdfplumber` 提取研报正文，通过滑动窗口（500字/50字重叠）进行语义分片。
2.  **嵌入层 (Embedding)**：调用硅基流动 (SiliconFlow) 的 `BAAI/bge-large-zh-v1.5` 将文本向量化。
3.  **存储层**：使用轻量级向量库 `ChromaDB` 进行持久化存储。
4.  **检索层**：结合向量空间检索与 `BGE-Reranker` 提升关键段落的召回权重。
5.  **生成层**：基于 `Qwen2.5-7B/72B` 大模型生成严谨的金融分析回答。

---

## 🚀 快速开始

### 1. 克隆项目
```bash
git clone https://github.com/haitanghuaweimianTom/Finance-RAG-Assistant-RAG-.git
cd Finance-RAG-Assistant-RAG-
```

### 2. 配置 API Key
在项目根目录新建 `.env` 文件，内容如下：
```env
SILICONFLOW_API_KEY=你的硅基流动API_KEY
```

### 3. 一键部署 (Windows 用户)

提供了完善的脚本，按顺序运行即可：

1.  **`Install.bat`**：自动检测 Python 并安装所有依赖环境。
2.  **放置资料**：将你的 `.pdf` 研报放入 `data-rawpdf` 文件夹。
3.  **`Update.bat`**：处理 PDF 并将其存入向量数据库。
4.  **`Start.bat`**：启动网页对话界面。

> **提示**：若想清空所有旧数据，请运行 `4.清空数据库.bat`。

---

## 📂 项目结构

```text
├── data-rawpdf/         # 原始PDF存放处
├── .env                 # API配置文件 (Git已忽略)
├── requirements.txt     # 依赖清单
├── 1.Install.bat        # 环境安装
├── 2.Start.bat          # 启动助手
├── 3.Update.bat         # 数据库更新
├── 4.Reset.bat          # 数据清空
├── data_process.py      # PDF清洗与切片逻辑
├── build_vector_db.py   # 向量化与ChromaDB交互
├── rag_chain.py         # RAG检索逻辑核心
└── frontend.py          # Streamlit网页界面
```

---

## 💡 免责声明
本项目生成的回答基于研报内容提取，不构成任何投资建议。投资者应保持独立思考，审慎做出决策。

---

## 🤝 贡献与反馈
欢迎通过 Issue 或 Pull Request 为本项目做出贡献。
项目作者：[haitanghuaweimianTom](https://github.com/haitanghuaweimianTom)

